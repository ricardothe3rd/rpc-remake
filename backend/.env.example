# Server Configuration
HOST=0.0.0.0
PORT=8080

# Security (REQUIRED - change in production!)
APP_SECRET=your-app-secret-key-min-32-characters-change-this-in-production
APP_ID=remake-rpc-001

# Appstore
APPSTORE_URL=http://localhost:5000

# CORS (comma-separated list of allowed origins)
ALLOWED_ORIGINS=*

# Session
SESSION_TIMEOUT=3600

# Phase timeout configuration (seconds)
# Phase 1 (HMAC authentication) timeout
PHASE_1_TIMEOUT=30
# Phase 2 (app setup) timeout
PHASE_2_TIMEOUT=60
# Heartbeat timeout for stale session detection
HEARTBEAT_TIMEOUT=60
